class_name enemy_wave_item_c
extends Resource

@export var amount: int = 0
@export var delay: float = 0.0
@export var enemy_type: PackedScene
@export var frequency = 1.0 # every how many seconds an enemy is spawned
	
